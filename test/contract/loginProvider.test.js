// test/contract/loginProvider.test.js
// Este teste foi deixado como OPCIONAL (padrão: SKIP) porque depende do pactum-flow (docker-compose) rodando em localhost:8080.
// Para executar, remova o `.skip` e suba o servidor: `docker-compose up -d`

describe.skip('Contrato - Provider Login (opcional)', () => {
  it('placeholder', async () => {});
});
